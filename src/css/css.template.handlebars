{{#sprites}}.icon-{{name}}, {{/sprites}} {
  background-image: url({{#sprites}}{{{escaped_image}}}{{/sprites}});
}

{{#sprites}}
.icon-{{name}} {
  background-position: {{px.offset_x}} {{px.offset_y}};
  width: {{px.width}};
  height: {{px.height}};
}
{{/sprites}}